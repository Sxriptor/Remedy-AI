/**
 * Cloud sync has been disabled.
 * Games are now only stored locally, no remote merging.
 */
export const mergeWithRemoteGames = async () => {
  // Stub: No longer merging with remote games
  return Promise.resolve();
};
